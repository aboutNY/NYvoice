# tasklist.md

## 0. 進め方
- 実装は上から順に進める
- 各タスク完了時にチェックを更新する
- 仕様変更が発生した場合は先に `docs/` と `design.md` を更新する

## 1. 初期セットアップ
- [x] 1.1 Swift macOSアプリ（メニューバー常駐）の雛形を作成
- [x] 1.2 基本ディレクトリ構造を `docs/repository-structure.md` に合わせて作成
- [x] 1.3 設定永続化（ショートカット、モデル名）を実装
- [x] 1.4 ログ出力基盤（sessionId付き）を実装

## 2. セッション制御基盤
- [x] 2.1 `SessionController` と状態モデル（Idle/Recording/Transcribing/Correcting/Inserting/Error）を実装
- [x] 2.2 状態遷移ルールを実装し、無効遷移を防止
- [x] 2.3 セッション開始/停止イベントの受け口を実装

## 3. 録音機能
- [x] 3.1 `AudioCaptureService` を実装（録音開始/停止）
- [x] 3.2 録音データを一時ファイル化して受け渡す処理を実装
- [x] 3.3 マイク権限チェックと未許可時の案内を実装

## 4. UI（録音モーダル）
- [x] 4.1 録音中モーダルを実装
- [x] 4.2 モーダルに録音状態、経過時間、停止ボタンを実装
- [x] 4.3 エラー時/停止時にモーダルを正しく閉じる処理を実装

## 5. グローバルショートカット
- [x] 5.1 `ShortcutManager` を実装
- [x] 5.2 ショートカットで録音開始/停止トグル動作を実装
- [x] 5.3 不正なキー設定のバリデーションを実装

## 6. 文字起こし連携（Whisper.cpp）
- [x] 6.1 `WhisperAdapter` を実装（実行コマンド、入出力処理）
- [x] 6.2 モデル設定読み込みと未設定時エラー処理を実装
- [x] 6.3 実行失敗時のエラー分類とログ記録を実装

## 7. 文脈補正連携（Ollama）
- [x] 7.1 `OllamaAdapter` を実装（API呼び出し）
- [x] 7.2 意味維持を前提とした補正プロンプトを実装
- [x] 7.3 補正失敗時のフォールバック（生文字起こし採用）を実装

## 8. テキスト挿入
- [x] 8.1 `TextInsertionService` の直接挿入処理を実装
- [x] 8.2 挿入失敗時のクリップボードフォールバックを実装
- [x] 8.3 アクセシビリティ権限不足時の案内を実装

## 9. 設定画面
- [x] 9.1 ショートカット設定UIを実装
- [x] 9.2 Whisper/Ollamaモデル設定UIを実装
- [x] 9.3 補正ON/OFF切替を実装

## 10. 統合と品質確認
- [x] 10.1 正常系E2E（開始→停止→文字起こし→補正→挿入）を確認
- [x] 10.2 異常系（権限不足、Whisper失敗、Ollama失敗、挿入失敗）を確認
- [x] 10.2.1 Accessibility 判定の誤検知（許可済みでも未許可表示）を修正
- [ ] 10.3 主要アプリ（チャット/エディタ）で挿入互換性を確認
- [x] 10.4 一時ファイル削除、ログ出力、復帰動作を確認

## 11. 配布準備（MVP）
- [x] 11.1 `.app` 配布パッケージ方針を確定
- [x] 11.2 初回起動時の依存・権限チェック導線を最終化
- [x] 11.3 チーム内配布手順をドキュメント化

## 12. 完了条件
- [ ] 12.1 MVP受け入れ基準をすべて満たす
- [ ] 12.2 `tasklist.md` の完了チェックを更新
- [ ] 12.3 必要な `docs/` 更新を反映

## 13. 開発リスト追加対応
- [x] 13.1 No.1 音声認識のデフォルト言語を日本語化（Whisper引数 `-l ja`）
- [x] 13.2 リリース反映手順の漏れ防止として `docs/distribution-guide.md` を更新
